export const metadata = {
  title: "Minihry | DarkNest"
};

export default async function Pageayout({ children }: { children: React.ReactNode }) {
  return (
      <>
        {children}
      </>
  );
}
